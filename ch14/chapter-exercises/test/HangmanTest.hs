module HangmanTest where

import Test.Hspec
-- import HangmanFunctions (Puzzle, fillInCharacter)

-- currentPuzzle = Puzzle "hat" [Just 'h', Nothing, Nothing] "eo"
--
hangmanTest :: IO ()
hangmanTest = return ()
-- hangmanTest = hspec $ do
--   describe "fillInCharacter" $ do
--     it "should add in character to end of guesses" $ do
--       -- fillInCharacter currentPuzzle 'm' `shouldBe` "meo"
--       filledInChars `shouldBe` "meo"
--       where (Puzzle _ _ filledInChars) = fillInCharacter currentPuzzle 'm'
